# ============================================================
# PrepEdital V2 — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o .env no git
# ============================================================

# ── AUTH ────────────────────────────────────────────────────
# Gerar com: openssl rand -base64 32
AUTH_SECRET="gere-uma-chave-segura-aqui"
NEXTAUTH_URL="https://SEU-DOMINIO.vercel.app"

# ── DATABASE ─────────────────────────────────────────────────
# Supabase: Settings → Database → Connection string → URI
DATABASE_URL="postgresql://postgres:SENHA@db.PROJETO.supabase.co:5432/postgres"

# ── ANTHROPIC (Claude) ────────────────────────────────────────
# https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY="sk-ant-api03-..."

# ── VOYAGE AI (Embeddings) ───────────────────────────────────
# https://dashboard.voyageai.com → API Keys (100M tokens grátis)
VOYAGE_API_KEY="pa-..."

# ── GOOGLE AUTH (opcional) ───────────────────────────────────
# https://console.cloud.google.com → Credentials → OAuth 2.0
# GOOGLE_CLIENT_ID="...apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="GOCSPX-..."

# ── QUEUE (Upstash QStash — para pipeline em produção) ────────
# https://console.upstash.com → QStash
# QSTASH_URL="https://qstash.upstash.io"
# QSTASH_TOKEN="..."

# ── INDEXAÇÃO (só script local, não vai para Vercel) ─────────
# BANK_ROOT="/Users/[usuario]/Downloads/PAI PROVAS"

# ── DEV ONLY — remover em produção ───────────────────────────
# DEV_BYPASS_AUTH="true"

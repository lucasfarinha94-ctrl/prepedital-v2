# ============================================================
# VARIÁVEIS DE AMBIENTE — Sistema V2
# Copie para .env e preencha com seus valores reais
# NUNCA commite o .env no git
# ============================================================

# ── DATABASE ─────────────────────────────────────────────────
# PostgreSQL + pgvector (Supabase recomendado)
DATABASE_URL="postgresql://postgres:[senha]@db.[ref].supabase.co:5432/postgres"

# ── NEXTAUTH ─────────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="[gere com: openssl rand -base64 32]"

# ── OAUTH ────────────────────────────────────────────────────
GOOGLE_CLIENT_ID="[do Google Cloud Console]"
GOOGLE_CLIENT_SECRET="[do Google Cloud Console]"

# ── SUPABASE STORAGE ─────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL="https://[ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[anon key do Supabase]"
SUPABASE_SERVICE_ROLE_KEY="[service role key — apenas server-side]"

# ── IA ───────────────────────────────────────────────────────
ANTHROPIC_API_KEY="sk-ant-[sua chave]"
OPENAI_API_KEY="sk-[sua chave]"

# ── QUEUE (Upstash QStash — opcional em dev) ─────────────────
QSTASH_URL="https://qstash.upstash.io"
QSTASH_TOKEN="[do Upstash Dashboard]"

# ── INDEXAÇÃO DO BANCO OFFLINE ───────────────────────────────
# Caminho absoluto para a pasta de conteúdo (só usado no script)
BANK_DIR="/Users/[usuario]/Downloads/PAI PROVAS/ÁREA FISCAL"

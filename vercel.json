{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --ignore-scripts",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 60
    }
  }
}
